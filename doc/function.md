# Functions
A function header may look like this:
```
fn public int example(string var1, string var2, ...)
```
## Any function without fn may return a syntax error, because the compiler will treat it as a var
Types are documented in data.md
